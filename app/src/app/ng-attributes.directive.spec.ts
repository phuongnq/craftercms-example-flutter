import { NgAttributesDirective } from './ng-attributes.directive';

describe('NgAttributesDirective', () => {
  it('should create an instance', () => {
    const directive = new NgAttributesDirective();
    expect(directive).toBeTruthy();
  });
});
